<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swufe.appinclass">

<!--    添加联网权限-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--        明文传输，网站服务器不提供https时只能这样-->
    <!--        android:usesCleartextTraffic="true"-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.swufe.appinclass.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Temperature" />
        <activity android:name=".Counter" />
        <activity android:name=".ExchangeRate" />
        <activity android:name=".ExchangeRate2" />
        <activity android:name=".MyListActivity" />
    </application>

</manifest>

<!-- activity内，有这部分才可以运行-->
<!--<intent-filter>-->
<!--<action android:name="android.intent.action.MAIN" />-->

<!--<category android:name="android.intent.category.LAUNCHER" />-->
<!--</intent-filter>-->